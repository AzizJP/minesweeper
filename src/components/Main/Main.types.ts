export enum Numbers {
  ZERO,
  ONE,
  TWO,
  THREE,
  FOUR,
  FIVE,
  SIX,
  SEVEN,
  EIGHT,
  NINE,
}

export enum SpriteTypes {
  NUMBER = 'number',
  FIELD_CELL = 'field-cell',
  EMOJI = 'emoji',
}

export interface ITime {
  firsDisplay: number;
  secondDisplay: number;
  thirdDisplay: number;
}
